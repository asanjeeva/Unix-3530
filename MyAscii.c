#include <stdio.h>
int main() {  
	    char c;
	        printf("Enter a character to display Ascii code: ");
		    scanf("%c", &c);  
		        
		        printf("ASCII value of %c = %d", c, c);
			    
			    return 0;
}
